<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A2A2A">

    <!-- Full-screen orb animation — drawn first (back layer) -->
    <com.dearmoon.shield.ui.OrbAnimationView
        android:id="@+id/orbView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- TOP: Mode label (like "EXERCISE" chip in reference) -->
    <TextView
        android:id="@+id/modeLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="56dp"
        android:text="ACTIVATING ROOT MODE"
        android:textColor="#CCFFFFFF"
        android:textSize="13sp"
        android:letterSpacing="0.15"
        android:textAllCaps="true"
        android:fontFamily="sans-serif-medium"
        android:paddingHorizontal="20dp"
        android:paddingVertical="8dp"
        android:background="@drawable/bg_mode_label_chip" />

    <!-- Main message — large, centres above the orb visual centre -->
    <TextView
        android:id="@+id/messageText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="108dp"
        android:layout_marginHorizontal="40dp"
        android:text="SHIELD is now\nwatching over you."
        android:textColor="#E0FFFFFF"
        android:textSize="26sp"
        android:textStyle="bold"
        android:gravity="center"
        android:lineSpacingMultiplier="1.3"
        android:fontFamily="sans-serif-light" />

    <!-- BOTTOM: Tap to skip hint -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="60dp"
        android:text="Tap anywhere to continue"
        android:textColor="#55AABBCC"
        android:textSize="12sp"
        android:letterSpacing="0.10"
        android:fontFamily="sans-serif" />

</FrameLayout>
