<?xml version="1.0" encoding="utf-8"?>
<!--
  activity_user_guide.xml
  Dark #0A0E1A background, DynamicIsland pill at top,
  ViewPager2 in the middle, frosted-circle → button at bottom.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_static_shield">

    <com.dearmoon.shield.ui.GuideBackgroundView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ══ Dynamic Island pill — top centre ══ -->
    <com.dearmoon.shield.ui.DynamicIslandView
        android:id="@+id/dynamicIsland"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ══ Main guide pager — fills space between island and button ══ -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/guidePager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/dynamicIsland"
        app:layout_constraintBottom_toTopOf="@id/nextButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ══ Next / arrow button — frosted circle, bottom centre ══ -->
    <ImageButton
        android:id="@+id/nextButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:background="@drawable/bg_button_frosted_circle"
        android:src="@drawable/ic_arrow_right"
        android:tint="#FFFFFF"
        android:scaleType="center"
        android:contentDescription="Next"
        android:stateListAnimator="@null"
        android:elevation="0dp"
        android:layout_marginBottom="40dp"
        app:layout_constraintBottom_toTopOf="@id/proceedCaption"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ══ "TAP TO PROCEED" caption below button ══ -->
    <TextView
        android:id="@+id/proceedCaption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="TAP TO PROCEED"
        android:textSize="10sp"
        android:textColor="#8892A4"
        android:letterSpacing="0.15"
        android:layout_marginBottom="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
